# Create a list of the first ten letters of the alphabet                                                                                       R.SOORYA SRI(192210404)
alphabet = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j']

# Print the first three letters from the list
print("First three letters:", alphabet[:3])

# Print any three letters from the middle
middle_start_index = len(alphabet) // 2 - 1  # Starting from the middle
middle_end_index = middle_start_index + 3   # Take three letters
print("Middle three letters:", alphabet[middle_start_index:middle_end_index])

# Print the letters from any particular index to the end of the list
start_index = 4  # Change this to the index from where you want to start
print("Letters from index", start_index, "to the end:", alphabet[start_index:])
OUTPUT:
First three letters: ['a', 'b', 'c']
Middle three letters: ['e', 'f', 'g']
Letters from index 4 to the end: ['e', 'f', 'g', 'h', 'i', 'j']
> 
